---
node_id: N9
node_type: ROUTER
hat: null
exec_type: inline
scale_gates: [STANDARD, DEEP]
kb_files: []
context_budget_lines: 50
input_ports:
  - from: N7
    signal_field: falsification_result
    required: true
output_ports:
  - to: N6
    signal_field: gate:S7_no_alternatives AND NOT N6_ran
    type: back-edge
  - to: N8
    signal_field: falsification_digest
    type: gate-open
required_output_sections:
  - "Routing Decision"
  - "Signal Flags Checked"
  - "Falsification Digest"
raises_signals: []
---

## CONTEXT LOADING

1. Read falsification_result from SIGNAL_STATE[(N7, falsification_result)].
2. Read `session.md` to check whether N6 is in executed_nodes.

## MACHINE ADVANTAGE

**Exploited:** Pure signal-state evaluation without cognitive contamination — the router reads signal flags and executed_nodes, makes one binary decision, and records it. No interpretation, no hedging.
**Instruction:** Evaluate the S7_no_alternatives gate condition exactly as stated. No partial evaluation. If the condition evaluates to true AND N6 is not in executed_nodes: enqueue N6. Unconditionally write falsification_digest (gate-open E17).
**Bypasses:** Human routing decisions can be influenced by recency bias or confirmation bias. The machine evaluates the condition purely from the signal state.

## PROTOCOL

N9 is a pure router: no hat, no ideation. Read signal state, make decision, record and emit.

1. **Read falsification_result from SIGNAL_STATE[(N7, falsification_result)].**

2. **Evaluate E16 gate condition:**
   `SIGNAL_STATE[(N7, falsification_result)].signal_flags includes 'S7_no_alternatives' AND 'N6' not in executed_nodes`

   - If TRUE: enqueue N6 to READY_SET (back-edge E16 fires). Record `"E16"` in `session.md.back_edges_fired`.
     Note: single-firing cap enforced — if N6 already in executed_nodes, condition is false by definition.
   - If FALSE: no back-edge. Continue.

3. **Write falsification_digest (gate-open E17 — unconditional):**
   The gate-open edge E17 (N9→N8) resolves unconditionally once N9 executes. Write `falsification_digest` to SIGNAL_STATE:
   ```
   routing_decision: <enqueue_N6 | passthrough>
   s7_flag_present: <true | false>
   n6_already_ran: <true | false>
   falsification_result_summary: <brief summary from N7's falsification_result>
   signal_flags: []
   ```

4. **Write `{session_dir}/stages/N9-router-record.md`** with:
   - Section `## Routing Decision` — decision taken and rationale (machine-readable)
   - Section `## Signal Flags Checked` — `S7_no_alternatives: present/absent`, `N6 in executed_nodes: yes/no`
   - Section `## Falsification Digest` — the falsification_digest payload

## SIGNAL OUTPUT

N9 emits ONE digest: `falsification_digest`.

```
routing_decision: <enqueue_N6 | passthrough>
s7_flag_present: <true | false>
n6_already_ran: <true | false>
falsification_result_summary: <brief>
signal_flags: []
```
