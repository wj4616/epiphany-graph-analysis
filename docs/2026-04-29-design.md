---
title: epiphany-graph-analysis — Skill Design
status: design-approved
date: 2026-04-29
authors: brainstorming session (user + Opus 4.7)
inputs:
  - ~/docs/epiphany/prompts/24-04-epiphany-graph-analysis-skill-spec.md (prompt-graph output)
  - ~/.claude/skills/epiphany-analysis/ (precedent v2.0.0)
  - ~/.claude/skills/epiphany-graph-genius/ (precedent v1.1.0)
output_target: ~/.claude/skills/epiphany-graph-analysis/
session_save_path: ~/docs/epiphany/graph-analysis/<session-id>/
version: 1.0.1
audit_log:
  - 2026-04-29 v1.0.0 — initial brainstorming output
  - 2026-04-29 v1.0.1 — deep audit pass: 17 issues fixed, 7 improvements added (canonical-section list, synonyms, S1 cutoffs, score ranges, re-run behavior, error catalog, module frontmatter schema, etc.)
---

# epiphany-graph-analysis — Skill Design

## Overview

A Graph-of-Thought reimplementation of `epiphany-analysis`. Takes an original text (Node A) and an analysis of that text (Node B), executes a 14-node signal-driven graph topology with parallel spawn nodes and inline role-switched blocks, and produces a rich artifact set: an enhanced version of Node A plus comprehensive cataloging of every step (analysis, ideas, accepted ideas, engineered solutions, synthesis decisions, verification audit, signal trace, execution trace).

**Design intent (in the user's words):**
> Take all of [the prompt-graph spec] into account, but the main idea is to completely enhance and revamp it for graph-of-thought-optimized pipeline and exploiting it, to produce the best end result. Produce more artifacts useful for human reviewers — ideas cataloged, solutions for each idea cataloged, full analysis at each step cataloged, capturing as much useful data as possible and saving it as .md formatted files during the process.

**Key departures from the prompt-graph input spec:**

1. **N5 LateralIdeate → N5.5 IdeaFilter → N8 SolutionEngineer** (was N5 → N8 with embedded filter). Three nodes, three artifacts: `ideas-catalog.md`, `accepted-ideas-catalog.md`, `solution-catalog.md`.
2. **N2 split into N2a + N2b as parallel spawns** (was single spawn with internal fan-out). Cleaner topology; truly independent A and B analyses; richer per-node stage files.
3. **MINIMAL mode dropped.** Two modes only: STANDARD (default) + DEEP. Fast-path users use original `epiphany-analysis`.
4. **N8 hybrid protocol** replaces `two-drafts-pick-best`: oppositional drafts → pick best → critique-and-refine (1 or 2 iterations).
5. **6 signals** (was 3): added S1_input_complexity, S4_xref_density, S5_idea_pool_thin.
6. **V1–V8 verification battery** (was R1–R6): adds V7 (graph-trace edge integrity) and V8 (topology-driven execution).
7. **`--resume` and `--retry-failed` flags** for replay-friendly operation.
8. **8 first-class user-facing markdown artifacts** in STANDARD (10 with synthesis-decisions and signal-trace, 12 in DEEP+expansion).
9. **`synthesis-decisions.md` artifact** to record N10 merge/drop decisions (replaces `merged_into` field on solution-catalog).
10. **Per-node stage files elevated to first-class artifacts** in `stages/` directory.

---

## Quick Reference

**Invoke:**
```
/epiphany-graph-analysis <node-a> [<node-b>] [--deep] [--quiet|--verbose]
                                              [--genius-threshold=N] [--drift-threshold=N]
/epiphany-graph-analysis --resume <session_dir>
/epiphany-graph-analysis --retry-failed <session_dir>
```

**Modes at a glance:**

| | STANDARD | DEEP |
|---|---|---|
| Default | yes | `--deep` |
| Active nodes | 13 | 14 |
| Spawn budget | ≤6 | ≤7 |
| Wall-clock | ≤22 min soft / 35 hard | ≤38 min soft / 55 hard |
| N8 iterations | 1 (or 2 if S1=high) | 2 always |
| Expansion (N12+E26) | inactive | conditional on S11 |
| Estimated cost | ~$0.20–0.35 | ~$0.40–0.70 |

**Artifacts (typical run):** `enhanced.md`, `analysis-report.md`, `ideas-catalog.md`, `accepted-ideas-catalog.md`, `solution-catalog.md`, `synthesis-decisions.md`, `self-audit.md`, `signal-trace.md`, `graph-trace.json` + 11–14 `stages/N*.md` files. DEEP-with-expansion adds `enhanced-first-pass.md` + `expansion-pass.md`. Edge cases produce `dropped-by-budget.md`.

**Hard gates:** HG-1 (output ≠ input), HG-2 (self-contained runtime), HG-3 (hybrid protocol per solution), HG-4 (fresh copy only), HG-5 (bounded iteration).

**Verification:** V1–V8 battery (V5/V7/V8 hard, others soft).

**Example:**
```bash
# Most common invocation: original draft + epiphany-genius output
/epiphany-graph-analysis ~/drafts/proposal.md ~/docs/epiphany/genius/proposal-analysis.md

# DEEP for thorough work
/epiphany-graph-analysis ~/drafts/proposal.md ~/docs/epiphany/genius/proposal-analysis.md --deep

# Resume after interrupt
/epiphany-graph-analysis --resume ~/docs/epiphany/graph-analysis/2026-04-29-abc123/

# Inline Node A (no source path) — Node B path required
/epiphany-graph-analysis "<paste content directly>" ~/path/to/analysis.md
```

---

## Section 1 — Architecture

### 1.1 Skill Identity

| Field | Value |
|---|---|
| Name | `epiphany-graph-analysis` |
| Trigger | `/epiphany-graph-analysis <path-to-A> [path-to-B]` |
| Skill path | `~/.claude/skills/epiphany-graph-analysis/` |
| Save path | `~/docs/epiphany/graph-analysis/<session-id>/` |
| Graph file | `~/.claude/skills/epiphany-graph-analysis/graph.json` |
| Schema file | `~/.claude/skills/epiphany-graph-analysis/graph.schema.json` |
| Modes | STANDARD (default) + DEEP (`--deep`) |
| Other flags | `--resume <session_dir>`, `--retry-failed <session_dir>`, `--quiet`, `--verbose`, `--genius-threshold=<int>` (default 8), `--drift-threshold=<int>` (default 3) |

### 1.2 Architecture Layers (three-layer rule)

1. **Orchestrator (`SKILL.md`)** — Main agent. Parses flags, loads `graph.json`, runs PRC1 validation, executes the ready-set scheduler (inline-to-fixpoint then parallel spawn fire), handles terminal logic, writes orchestrator-assembled artifacts (`analysis-report.md`, `signal-trace.md`, `graph-trace.json`, `session.json`), emits summary line. Never modifies `graph.json` mid-run.
2. **Graph registry (`graph.json`)** — Single source of truth for topology. When module frontmatter and `graph.json` disagree, **`graph.json` wins** (source-of-truth rule).
3. **Per-node modules (`modules/N*.md`)** — Each node has a PROTOCOL section. Inline modules executed via role-switched blocks; spawn modules dispatched via `Agent` tool.

### 1.3 Topology Summary

- **14 nodes**: N1, N2a, N2b, N3, N4, N5, N5.5, N6, N7, N8, N9, N10, N11, N12
- **26 edges**: E01–E26
- **5 spawn nodes**: N2a, N2b, N5, N7, N10. Everything else inline.
- **6 signals**: S1_input_complexity, S2_thin_B, S4_xref_density, S5_idea_pool_thin, S7_no_alternatives, S11_artifact_gap
- **3 back-edges** with single-firing caps: E09 (N2b→N6), E20 (N9→N6), E26 (N12→N10)

### 1.4 Spawn & Wall-Clock Budgets

| Mode | Active nodes | Spawn budget | Wall-clock soft / hard | Est. cost |
|---|---|---|---|---|
| STANDARD | 13 (all except N12) | ≤6 | 22 min / 35 min | ~$0.20–0.35 |
| DEEP (no expansion) | 14 (all) | ≤7 | 38 min / 55 min | ~$0.40–0.55 |
| DEEP (with E26 expansion) | 14 (all) | ≤7 | 38 min / 55 min | ~$0.50–0.70 |

**Spawn dispatch count:**
- STANDARD: 5 dispatches (N2a, N2b, N5, N7, N10) — budget +1 buffer
- DEEP no expansion: 5 dispatches — budget +2 buffer
- DEEP with expansion: **6 dispatches** (N2a, N2b, N5, N7, N10 first, N10 re-synth) — budget +1 buffer

Per-module hard halt: 10 min (15 min special-case for N8).

**Cost estimation methodology:** Based on Opus 4.7 pricing (~$15/M input, ~$75/M output) and typical token usage per spawn (~3000–6000 tokens per Agent call) plus inline-block context (~10000 tokens combined). Values are approximate; actual cost varies with Node A size and S1 complexity.

### 1.5 File Layout

```
~/.claude/skills/epiphany-graph-analysis/
├── SKILL.md                        # orchestrator
├── graph.json                      # topology source of truth
├── graph.schema.json               # programmatic validator
├── README.md
├── CHANGELOG.md
├── modules/
│   ├── N1-intake.md                # inline — IntakeDecompose + GeniusDetection
│   ├── N2a-analyze-a.md            # spawn — analyze Node A
│   ├── N2b-analyze-b.md            # spawn — analyze Node B
│   ├── N3-section-tailor.md        # inline (conditional)
│   ├── N4-cross-reference.md       # inline
│   ├── N5-lateral-ideate.md        # spawn
│   ├── N5.5-idea-filter.md         # inline
│   ├── N6-defixation.md            # inline (conditional)
│   ├── N7-adversarial.md           # spawn
│   ├── N8-solution-engineer.md     # inline (hybrid protocol)
│   ├── N9-router.md                # inline
│   ├── N10-synthesize.md           # spawn
│   ├── N11-verify.md               # inline
│   └── N12-expand.md               # inline (DEEP only)
├── kb/                             # 12 KB files
│   ├── analysis-methodology.md
│   ├── input-preloading-templates.md
│   ├── section-tailoring-map.md
│   ├── domain-catalog.md
│   ├── debono-techniques.md
│   ├── ohlsson-defixation.md
│   ├── falsification-checklists.md
│   ├── verification-gates.md
│   ├── oppositional-drafting-axes.md   # NEW
│   ├── elegance-rubric.md              # ported from graph-genius
│   ├── vocabulary-rubric.md            # ported from graph-genius
│   └── blend-template.md               # ported from graph-genius
├── scripts/
│   ├── session-init.sh
│   ├── validate-graph.py
│   └── test-runner.sh
└── tests/
    ├── fixtures/
    ├── results/                    # phase-6 test results: <date>-results.md
    └── {unit,smoke,signal,hg,verification,regression,replay}/
```

**Stage file naming convention:** stage files in `{session_dir}/stages/` use full descriptive hyphenated names (e.g., `N1-intake-decompose.md`, `N4-cross-reference.md`); module files in `{skill_path}/modules/` use short names (e.g., `N1-intake.md`, `N4-cross-reference.md`). Module file = the protocol; stage file = the runtime output.

**Session output structure** (`~/docs/epiphany/graph-analysis/<session-id>/`):

```
<session-id>/
├── input-a.md                      # verbatim Node A
├── input-b.md                      # verbatim Node B (or inline-substitute)
├── stages/                         # 11–14 per-node stage files
│   ├── N1-intake-decompose.md
│   ├── N2a-analyze-a.md
│   ├── N2b-analyze-b.md
│   └── ...
├── enhanced.md                     # final enhanced Node A (N10)
├── enhanced-first-pass.md          # DEEP+E26: preserved before re-synth
├── analysis-report.md              # cross-cutting analysis (orchestrator)
├── ideas-catalog.md                # raw ideas (N5)
├── accepted-ideas-catalog.md       # filtered ideas (N5.5)
├── solution-catalog.md             # engineered solutions (N8)
├── synthesis-decisions.md          # merge/drop decisions (N10)
├── self-audit.md                   # V1–V8 verification (N11)
├── expansion-pass.md               # DEEP+S11: thin-spot expansions (N12)
├── signal-trace.md                 # human-readable signal narrative
├── graph-trace.json                # structured execution trace
└── session.json                    # run-state metadata for replay
```

**Artifact totals:**
- STANDARD: 10 markdown user-facing + 1 user-facing JSON + 1 metadata JSON (session.json) + 11–13 stage files
- DEEP-no-expansion: same as STANDARD
- DEEP-with-expansion: 12 markdown + 1 JSON + session.json + 12–14 stage files

---

## Section 2 — Node Registry

### 2.1 Node Table

| ID | Name | Type | Exec | Hat | Lines | Modes | Raises | KB Files |
|---|---|---|---|---|---|---|---|---|
| **N1** | IntakeDecompose | DECOMPOSE | inline | Einstein/Feynman | 200 | S, D | S1_input_complexity | input-preloading-templates, vocabulary-rubric |
| **N2a** | AnalyzeA | ANALYZE-A | **spawn** | Tesla (measurement) | 1500 | S, D | — | analysis-methodology, vocabulary-rubric |
| **N2b** | AnalyzeB | ANALYZE-B | **spawn** | Darwin (variation-survey) | 1500 | S, D | S2_thin_B | analysis-methodology, section-tailoring-map |
| **N3** | SectionTailor | TAILOR | inline (cond.) | Feynman | 600 | S, D | — | section-tailoring-map |
| **N4** | CrossReference | XREF | inline | Tesla (bridge-builder) | 800 | S, D | S4_xref_density | — |
| **N5** | LateralIdeate | LATERAL | **spawn** | de Bono | 1200 | S, D | — | domain-catalog, debono-techniques, blend-template, elegance-rubric |
| **N5.5** | IdeaFilter | FILTER | inline | Boden | 600 | S, D | S5_idea_pool_thin | verification-gates, elegance-rubric |
| **N6** | Defixation | DEFIX | inline (cond.) | Ohlsson | 700 | S, D | — | ohlsson-defixation |
| **N7** | AdversarialVerify | ADVERSARIAL | **spawn** | Popper + Millikan | 1500 | S, D | S7_no_alternatives | falsification-checklists |
| **N8** | SolutionEngineer | ENGINEER | inline | Feynman + Boden | 2000 | S, D | — | verification-gates, oppositional-drafting-axes |
| **N9** | Router | ROUTER | inline | — | 50 | S, D | — | — |
| **N10** | Synthesize | SYNTHESIS | **spawn** | Feynman + Boden(synthesizer) | 2000 | S, D | — | — |
| **N11** | Verify | VERIFY | inline | Popper | 600 | S, D | S11_artifact_gap | verification-gates |
| **N12** | Expand | EXPANSION | inline (cond.) | Feynman | 800 | **D only** | — | — |

**Numerical sort key** for deterministic tiebreaking: N1=1.0, N2a=2.0, N2b=2.1, N3=3.0, N4=4.0, N5=5.0, N5.5=5.5, N6=6.0, N7=7.0, N8=8.0, N9=9.0, N10=10.0, N11=11.0, N12=12.0.

### 2.2 Key Node Behaviors

#### N1 IntakeDecompose
- Reads Node A and Node B; writes verbatim to `input-a.md` and `input-b.md` (inline Node A → records `node_a_source: 'inline'`).
- **Genius Detection** on Node B: counts canonical-or-synonym headers from the 10-section Section-Tailoring Map. See **Appendix C** for the canonical section list and accepted synonyms. Matching is case-insensitive, ignores trailing punctuation, accepts both `## Header` and bold-line variants. ≥`--genius-threshold` (default 8) → `genius-current`; ≥`--drift-threshold` (default 3) and < genius-threshold → `genius-drift`; else → `generic-fallback`.
- Computes **S1_input_complexity** via formula then maps to bucket:
  - `complexity_score = log10(line_count) × structural_depth × (1 + constraint_density)` where:
    - `line_count` = Node A line count
    - `structural_depth` = max markdown header nesting depth (1 for flat, 2 for `##`, 3 for `###`, etc.)
    - `constraint_density` = count of MUST/SHALL/REQUIRED/FORBIDDEN/NEVER imperative keywords per 100 lines
  - **Buckets:** `score < 4` → `low`; `4 ≤ score < 12` → `medium`; `score ≥ 12` → `high`
  - Examples: 100-line flat doc with 2 MUSTs → `log10(100) × 1 × 1.02 ≈ 2.04` → `low`. 1500-line nested doc with 30 MUSTs → `log10(1500) × 4 × 3 ≈ 38` → `high`.
- **HG-1 enforcement**: verifies `enhanced.md` target path ≠ Node A source path (trivially satisfied for inline Node A). HALT on equality.
- Required output sections: `Input Class`, `HG-1 Verification`, `Node A Decomposition`, `Node B Genius Detection`, `Complexity Signal`, `Session Init`.

#### N2a AnalyzeA + N2b AnalyzeB (parallel spawn pair)
- **N2a (Tesla hat)**: structural decomposition, key claims, apparent gaps, strengths, weaknesses on Node A. Writes `N2a-analyze-a.md`.
- **N2b (Darwin hat)**: variation survey on Node B. Genius-section processing if `genius-current/drift`, flat findings if `generic-fallback`. Raises `S2_thin_B` if Node B is generic-fallback OR <3 canonical sections. Writes `N2b-analyze-b.md`.
- N2b required output includes `Genius Detection Corroboration` to validate N1's classification.
- Both fire in parallel via ready-set scheduler — single Agent message with two parallel tool calls.

#### N5.5 IdeaFilter
- Reads `ideas-catalog.md` from N5 and `xref_map` from N4.
- Applies pro/con/utility filter using `verification-gates.md` criteria. Each idea scored on three axes (integer 1–5, where 5 is best):
  - **relevance** — does it map to a verifiable Node A section? (1 = unrelated, 5 = directly addresses an A-section)
  - **feasibility** — can it be drafted as a concrete change? (1 = aspirational, 5 = clear concrete patch)
  - **non-redundancy** — does it differ meaningfully from already-accepted ideas? (1 = duplicate, 5 = novel angle)
- **Status assignment**:
  - **ACCEPT** — all three scores ≥ 3 AND `relevance + feasibility + non-redundancy ≥ 11`
  - **ACCEPT-CONDITIONAL** — all three scores ≥ 2 AND total ≥ 8 AND not ACCEPT
  - **REJECT** — any score = 1 OR total < 8
- Writes `accepted-ideas-catalog.md` with each idea's three scores, status, rejection rationale (if REJECT), and `pass: original | supplemental` field.
- **Inline supplemental brainstorm**: if `accepted_count < ⌈xref_count × 0.4⌉`, raises `S5_idea_pool_thin` and runs a brief role-switched supplemental block targeting **specifically the B-findings that have no ACCEPT/CONDITIONAL idea mapped to them** (the `unmapped_findings` set). The supplemental block is constrained: it generates 1–3 ideas per unmapped finding, no broader exploration. These get assigned `pass: supplemental` and `xref_via: derived` (since N4's xref_map predates them; their xref entries are reverse-derived from the source B-finding they target). Re-applies filter on combined pool. Bounded ≤1 supplemental pass.

#### N8 SolutionEngineer (hybrid protocol — see Section 5 for full detail)
For each accepted idea: pick oppositional axis → draft A and draft B independently → compare and pick winner → critique against B-finding + Node A constraint → refine → (DEEP or S1=high: critique again, refine again) → final.

#### N12 Expand (DEEP only)
- Activates only on `S11_artifact_gap` from N11 in DEEP mode (E25).
- Identifies thin-spot sections in `enhanced.md`, writes `expansion-pass.md`.
- E26 back-edge fires once: feeds expansion deltas to N10 for re-synthesis. N10 renames first-pass `enhanced.md` to `enhanced-first-pass.md` before writing expanded version.

### 2.3 Spawn-vs-Inline Justification

| Decision | Rationale |
|---|---|
| N2a, N2b spawns | High-context analysis; truly independent inputs warrant parallel dispatch |
| N5 spawn | Lateral ideation needs full 1200-line context for KB |
| N7 spawn | Adversarial verification considers all candidates against falsification checklists |
| N10 spawn | Synthesis integrates 6+ upstream outputs; needs 2000 lines |
| N8 inline (2000 lines) | Hybrid protocol benefits from continuous context across solutions; saves 1 spawn; 2000-line budget matches a spawn's allocation |
| N3, N5.5, N9, N11 inline | Cheaper operations where role-switched blocks suffice |
| N6, N12 inline | Conditional control-flow nodes; small contexts; no parallelism benefit |

---

## Section 3 — Edges, Signals & Scheduling

### 3.1 Edge Table

| ID | Source → Target | Type | Signal Field | Modes | Gate Condition |
|---|---|---|---|---|---|
| E01 | input → N1 | required | — | S, D | — |
| E02 | N1 → N2a | required | intake_digest | S, D | — |
| E03 | N1 → N2b | required | intake_digest | S, D | — |
| E04 | N1 → N3 | optional | intake_digest | S, D | — |
| E05 | N1 → N4 | optional | intake_digest | S, D | — |
| E06 | N2a → N4 | required | analysis_a_digest | S, D | — |
| E07 | N2b → N4 | required | analysis_b_digest | S, D | — |
| E08 | N2b → N3 | forward-conditional | analysis_b_digest | S, D | `intake_digest.node_b_type ∈ {genius-current, genius-drift}` |
| E09 | N2b → N6 | **back-edge** | gate:S2_thin_B | S, D | `analysis_b_digest.signal_flags ∋ S2_thin_B` |
| E10 | N3 → N4 | required (when N3 active) | tailored_b_digest | S, D | — |
| E11 | N4 → N5 | required | xref_map | S, D | — |
| E12 | N4 → N8 | required | xref_map | S, D | — |
| E13 | N5 → N5.5 | required | ideas_digest | S, D | — |
| E14 | N5.5 → N8 | required | accepted_ideas_digest | S, D | — |
| E15 | N5.5 → N7 | required | accepted_ideas_digest | S, D | — |
| E16 | N6 → N8 | optional | breakthrough_digest | S, D | — |
| E17 | N6 → N7 | optional | breakthrough_digest | S, D | — |
| E18 | N7 → N9 | required | falsification_result | S, D | — |
| E19 | N7 → N8 | optional | adversarial_digest | S, D | — |
| E20 | N9 → N6 | **back-edge** | gate:S7_no_alternatives ∧ ¬N6_ran | S, D | `falsification_result.signal_flags ∋ S7_no_alternatives ∧ N6 ∉ executed_nodes` |
| E21 | N9 → N8 | gate-open | falsification_digest | S, D | N9 executed |
| E22 | N8 → N10 | required | solutions_digest | S, D | — |
| E23 | N10 → N11 | required | enhanced_draft | S, D | — |
| E24 | N11 → output | terminal | — | S, D | `verification_report.signal_flags ∌ S11_artifact_gap` OR mode=STANDARD OR pass=2 |
| E25 | N11 → N12 | forward-conditional (single-firing cap) | first_pass_verified | D | `verification_report.signal_flags ∋ S11_artifact_gap ∧ mode=DEEP ∧ pass=1` |
| E26 | N12 → N10 | **back-edge** | expansion_digest | D | N12 completed with expansions |

**Edge-type semantics:**
- **required**: target activates only when source has written SIGNAL_STATE entry. Required edges from inactive sources are dropped from working topology.
- **optional**: never blocks activation; consumed if present.
- **forward-conditional**: gate condition evaluated at ready-set compute time; blocks if false.
- **back-edge**: requires explicit enqueue from source's post-execution check. Single-firing cap.
- **gate-open**: resolves unconditionally once source executes (required-equivalent).
- **terminal**: pipeline exit.

**Gate conditions can read any SIGNAL_STATE entry**, not just the edge's source (e.g., E08's gate reads `intake_digest.node_b_type` which originates at N1).

**E24 vs E25 are mutually exclusive**: N11 fires exactly one per pass.

### 3.2 Signal System

| Signal ID | Raised By | Condition | Type | Consumed By | Effect |
|---|---|---|---|---|---|
| **S1_input_complexity** | N1 | `line_count × structural_depth × constraint_density` → {low, medium, high} | tuning | N5 (ideation breadth), N8 (iteration count) | Adjusts node-internal parameters |
| **S2_thin_B** | N2b | Node B `generic-fallback` OR <3 canonical sections | gate | E09 → N6 | Triggers early defixation |
| **S4_xref_density** | N4 | `xref_count / max(node_a_constraint_count, 1)` → {sparse, normal, dense} | tuning | N5 (lateral_pass_count: sparse=3, normal=2, dense=1) | Adjusts ideation breadth |
| **S5_idea_pool_thin** | N5.5 | `accepted_count < ⌈xref_count × 0.4⌉` | informational | N5.5 itself | Triggers inline supplemental brainstorm; logged in trace |
| **S7_no_alternatives** | N7 | All accepted ideas survive falsification with zero counter-examples | gate | E20 → N6 (if N6 not run) | Triggers late defixation |
| **S11_artifact_gap** | N11 | V6 thin-spot detection (DEEP mode, pass=1 only) | gate | E25 → N12 | Triggers expansion |

**Signal classes:**
- **Tuning** (S1, S4): adjust internal node parameters; never gate edges. Logged in `signal-trace.md`.
- **Gate** (S2, S7, S11): drive routing via gate-condition edges.
- **Informational** (S5): neither gates an edge nor tunes a downstream node; triggers in-node behavior only.

**SIGNAL_STATE invariant (PRC2):** Append-only per `(node_id, signal_field)` key. Distinct signal_fields from same node allowed (e.g., N7 writes both `falsification_result` and `adversarial_digest`).

### 3.3 Ready-Set Scheduling Protocol

```
1. compute_ready_set(SIGNAL_STATE, executed_nodes, enqueued_back_edges)
   for each active node not yet executed:
     - all required edges from active sources have SIGNAL_STATE entries? AND
     - all forward-conditional edges' gate conditions satisfied? AND
     - if back-edge-only target, is it explicitly enqueued?
     → eligible

2. inline-to-fixpoint:
   while ready_set contains inline nodes:
     pick first eligible inline node (by ascending sort_key)
     execute role-switched block per modules/N*.md
     write output to stages/, write SIGNAL_STATE entries
     check for back-edge enqueue triggers (E09, E20, E26)
     recompute ready_set

3. spawn_fire:
   if ready_set contains spawn nodes:
     dispatch ALL eligible spawn nodes in parallel
     (single Agent() message with N tool-call blocks)
     await all returns
     write outputs to stages/, write SIGNAL_STATE entries
     check for back-edge enqueue triggers
     recompute ready_set

4. terminate when N11 reaches output (E24) or hard-halt timeout
```

**Sync rules:**
- **Early back-edge sync (S2_thin_B)**: if N2b raised S2_thin_B and N6 has not yet completed, N7 is blocked from firing — prevents missing `breakthrough_digest` via E17.
- **Late back-edge sync (S7_no_alternatives)**: when N9 enqueues N6 via E20, N6 is added to N8's pending-required set for that run — N8 waits for both N9 AND N6.

**SIGNAL_STATE persistence for resume:**
SIGNAL_STATE is held in-memory during execution AND persisted incrementally to `session.json.signal_state` after each node completes. This makes `--resume <session_dir>` reconstructible from disk: the orchestrator loads `session.json`, restores SIGNAL_STATE, restores `executed_nodes`, and re-enters the ready-set loop. The full snapshot is also written to `graph-trace.json.signal_state_snapshot` at terminal for replay-debugging purposes. Per node completion → write to both session.json (for resume) and update in-memory dict (for runtime).

**Concurrency note:** The orchestrator is single-threaded. "Parallel spawn fire" means multiple `Agent()` tool calls dispatched in a single message; the orchestrator awaits all returns and then processes them sequentially. SIGNAL_STATE writes are sequential — no race conditions possible.

### 3.4 Execution Stages (typical STANDARD run)

> Note: "Execution Stages" are runtime ready-set groupings; do not confuse with the 6 Implementation Phases in §9.2.

| Stage | Inline-to-fixpoint | Spawn fire |
|---|---|---|
| S0 | N1 | — |
| S1 | — | N2a ‖ N2b |
| S2 | N3 (if E08 gate true), N4, N6 (if E09 fires) | — |
| S3 | — | N5 |
| S4 | N5.5 (incl. supplemental if S5) | — |
| S5 | — | N7 |
| S6 | N9, N6 (if E20 fires and not yet run), N8 | — |
| S7 | — | N10 |
| S8 | N11 | — |
| S9 (DEEP+S11 only) | N12 | — |
| S10 (DEEP+S11 only) | — | N10 (re-synth) |
| S11 (DEEP+S11 only) | N11 (re-verify) | — |
| terminal | output (E24) | — |

Inline nodes within a stage execute serially; spawn nodes within a stage fire in parallel.

### 3.5 PRC1 Validation (pre-execution)

Before execution begins:

1. **DAG check** (excluding back-edges E09, E20, E26): topological sort visits every active node.
2. **Edge resolution**: every active edge's source/target resolves to declared active node IDs (or `input`/`output` sentinels).
3. **Signal field validity**: every active edge's signal_field matches a declared digest name, gate literal (`gate:`), or `—`.
4. **Connectivity**: active subgraph (back-edges INCLUDED) is connected with N1 as source and `output` (via E24) as sink.
5. **KB file existence**: every node's referenced KB file exists at `kb/<filename>`.
6. **graph.schema.json validation**: `graph.json` validates against schema.

HALT with explicit error on any failure.

---

## Section 4 — Artifacts & Schemas

### 4.1 Artifact Inventory

| Artifact | Writer | Always present? |
|---|---|---|
| `input-a.md` | N1 | ✓ |
| `input-b.md` | N1 | ✓ |
| `enhanced.md` | N10 | ✓ |
| `enhanced-first-pass.md` | N10 | DEEP + E26 |
| `analysis-report.md` | orchestrator | ✓ |
| `ideas-catalog.md` | N5 | ✓ |
| `accepted-ideas-catalog.md` | N5.5 | ✓ |
| `solution-catalog.md` | N8 | ✓ |
| `synthesis-decisions.md` | N10 | ✓ |
| `self-audit.md` | N11 | ✓ |
| `expansion-pass.md` | N12 | DEEP + S11 |
| `dropped-by-budget.md` | N8 | only if N8 budget defer triggered |
| `signal-trace.md` | orchestrator | ✓ |
| `graph-trace.json` | orchestrator | ✓ |
| `session.json` | orchestrator | ✓ |
| `stages/N*.md` | each node | per-node |

**Totals:**
- STANDARD: 10 markdown user-facing + 1 JSON user-facing + 1 metadata JSON + 11–13 stage files
- DEEP-with-expansion: 12 markdown + 1 JSON + 1 metadata JSON + 12–14 stage files

### 4.2 enhanced.md (written by N10)

```yaml
---
session_id: "2026-04-29-abc123"
node_a_source: "/path/to/original-input.md" | "inline"
node_b_source: "/path/to/analysis.md" | "inline-substitute"
node_b_type: "genius-current" | "genius-drift" | "generic-fallback" | "inline-substitute"
scale: "STANDARD" | "DEEP"
synthesis_pass: 1 | 2
integrated_solution_count: <int>
total_solutions_in_catalog: <int>
verification_status: "PASS" | "PARTIAL" | "FAIL"
superseded_by: null
timestamp: "ISO-8601"
---
```

**Body:** enhanced version of Node A's content with all accepted solutions integrated. Verbatim preservation of Node A's directives (AP-1). Trailing `## Enhancement Summary` section listing per-solution `id, applied_change, source_b_finding, target_section_in_a`.

### 4.3 enhanced-first-pass.md

Identical schema to enhanced.md, with `synthesis_pass: 1` always and `superseded_by: "enhanced.md"`. Written when E26 fires.

### 4.4 analysis-report.md (orchestrator-assembled)

```yaml
---
session_id: "..."
node_a_source: "..."
node_b_source: "..."
node_b_type: "..."
a1_findings_count: <int>
b1_findings_count: <int>
xref_count: <int>
xref_density: "sparse" | "normal" | "dense"
---
```

**Body sections:**
1. `## Section 1: Node A Analysis (A1)` — from `stages/N2a-analyze-a.md`
2. `## Section 2: Node B Analysis (B1)` — from `stages/N2b-analyze-b.md` (+ `stages/N3-section-tailor.md` if N3 ran)
3. `## Section 3: Cross-Reference Map` — from `stages/N4-cross-reference.md`. Table columns: `b_finding_id` (e.g., `B07`), `a_section` (e.g., `§4.2`), `relevance_score` (integer 1–5, where 5 = direct match), `potential_action` (free-text, 1-line summary of how the B-finding could improve the A-section).
4. `## Section 4: Genius Detection` — N1 classification + N2b corroboration

### 4.5 ideas-catalog.md (N5)

```yaml
---
session_id: "..."
total_raw_ideas: <int>
domains_traversed: [<domain1>, ...]
lateral_pass_count: <int>  # 1, 2, or 3 from S4
s1_complexity_at_runtime: "low" | "medium" | "high"
---
```

Per-idea: `{id: I0N, source_b_finding, domain, technique, description, verbatim_preservation_check: PASS|FAIL}`.

### 4.6 accepted-ideas-catalog.md (N5.5)

```yaml
---
session_id: "..."
total_input_ideas: <int>
accepted_count: <int>
conditional_count: <int>
rejected_count: <int>
supplemental_pass_fired: true | false
s5_idea_pool_thin_raised: true | false
---
```

**Body sections:**
1. `## Filter Criteria Applied` — describes the three score axes (relevance, feasibility, non-redundancy) each scored 1–5; ACCEPT/CONDITIONAL/REJECT thresholds (per §2.2 N5.5)
2. `## Original-Pass Accepted Ideas` — entries with `pass: original` and `status ∈ {ACCEPT, ACCEPT-CONDITIONAL}`
3. `## Original-Pass Rejected Ideas` — entries with `pass: original` and `status: REJECT`, including rejection rationale
4. `## Supplemental Pass` (only if S5 fired) — entries with `pass: supplemental` and `xref_via: derived`

Per-idea entry schema:
```
- id: I0X
- pass: original | supplemental
- xref_via: original | derived  # only set for supplemental
- relevance_score: 1–5
- feasibility_score: 1–5
- non_redundancy_score: 1–5
- score_total: <int>
- status: ACCEPT | ACCEPT-CONDITIONAL | REJECT
- rejection_reason: <text>  # only if REJECT
- target_section_in_a: <ref>
- source_b_finding: <id>
```

### 4.7 solution-catalog.md (N8) — most detailed

```yaml
---
session_id: "..."
total_accepted_ideas: <int>
solutions_finalized: <int>
solutions_dropped: <int>
total_critique_iterations: <int>
mode: "STANDARD" | "DEEP"
s1_complexity: "low" | "medium" | "high"
---
```

Per-solution entry:
```
### S<NN>: <title>
- source_idea_id: I0X
- target_section_in_a: <ref>
- oppositional_axis: <A0–A6>
- draft_A: <text>
- draft_B: <text>
- comparison_rationale: <text>
- winner: A | B
- critique_1: <text>
- refined_v1: <text>
- critique_2: <text> (only if iterations=2)
- refined_v2: <text> (only if iterations=2 and critique_2 found defects)
- final: <refined_v1 | refined_v2>
- pro: [list]
- con: [list]
- utility: high | medium | low
- status: ACCEPT | ACCEPT-CONDITIONAL | REJECT | DROPPED
```

### 4.7.1 N7 stage file: adversarial-verify.md schema

N7 produces two SIGNAL_STATE outputs (`falsification_result` for E18, `adversarial_digest` for E19) backed by one stage file `stages/N7-adversarial-verify.md`:

**Frontmatter:** standard stage frontmatter (per §4.15) + `falsification_summary: {tested_count, surviving_count, falsified_count, s7_no_alternatives_raised: bool}`.

**Body sections:**
1. `## Methodology` — which falsification checklists from `falsification-checklists.md` were applied
2. `## Per-Idea Adversarial Results` — for each accepted idea:
   ```
   - source_idea_id: I0X
   - test_attempts: [<test_name>, ...]  # checklist tests applied
   - counter_examples_found: [<text>, ...]  # 0+ entries
   - weaknesses_identified: [<text>, ...]  # 0+ entries
   - verdict: "survives" | "falsified" | "weakened"  # weakened = survives but with caveats
   ```
3. `## Aggregate Findings` — summary used as `adversarial_digest` payload for E19 → N8

**Signal raise:** `S7_no_alternatives` raised iff every accepted idea has `verdict: "survives"` AND `counter_examples_found: []` AND `weaknesses_identified: []` (i.e., adversarial pass found nothing to push back on — possible sign that defixation may help surface alternatives).

### 4.8 synthesis-decisions.md (N10)

```yaml
---
session_id: "..."
decisions_count: <int>
contradictions_resolved: <int>
merges_performed: <int>
drops_performed: <int>
---
```

Per-decision: `{decision_type: 'merge'|'drop'|'reorder'|'no_change', source_solution_ids: [list], target_id: <id>|null, rationale: <text>}`.

### 4.9 self-audit.md (N11)

```yaml
---
session_id: "..."
overall_verdict: "PASS" | "PARTIAL" | "FAIL"
check_count: 8
fail_count: <int>
verification_pass: 1 | 2
---
```

Per-check entry (V1–V8) with PASS/PARTIAL/FAIL + detail. See Section 7 for definitions. V6 verifies "all mandatory artifacts (per §7.2 V6 list) exist with current timestamps."

### 4.10 expansion-pass.md (N12, DEEP only)

```yaml
---
session_id: "..."
thin_spots_identified: <int>
expansion_targets: [<a_section_ref>, ...]
---
```

Per-thin-spot entry (structured for N10 actionability):
```
- target_section: <e.g., §4.2>
- gap_description: <prose, 1-3 sentences identifying what's underdeveloped>
- proposed_depth_pass:
    type: "elaborate" | "add_example" | "add_counter_case" | "expand_constraint" | "fill_subsection"
    instruction: <prose, 2-5 sentences telling N10 what to inject>
    target_b_finding_ids: [<id>, ...]  # B-findings that should drive the expansion
    insertion_locus: "before" | "after" | "within"  # relative to target_section
```

The structured `proposed_depth_pass` is required so N10 has actionable instructions, not vague text. N10 reads each entry, locates the target_section in the existing `enhanced.md`, and applies the expansion via the named type (elaborate, add_example, etc.).

### 4.11 signal-trace.md (orchestrator, human-readable)

```markdown
# Signal Trace — <session_id>

## Tuning Signals (informational)
- S1_input_complexity raised by N1 at <ts>: value=<low/med/high>, formula=<details>
- S4_xref_density raised by N4 at <ts>: value=<sparse/normal/dense>, xref_count=<n>

## Gate Signals (routing-affecting)
- S2_thin_B: <fired by N2b at ts | not raised>; if fired → triggered E09 → N6
- S5_idea_pool_thin: <fired by N5.5 at ts | not raised>; if fired → inline supplemental
- S7_no_alternatives: <fired by N7 at ts | not raised>
- S11_artifact_gap: <fired by N11 at ts | not raised>

## Back-Edge Firings
- E09 (N2b→N6): fired/not fired
- E20 (N9→N6): fired/not fired
- E26 (N12→N10): fired/not fired
- Total back-edge firings: 0/1/2
```

### 4.12 graph-trace.json (orchestrator, structured)

```json
{
  "session_id": "...",
  "scale": "STANDARD" | "DEEP",
  "node_b_type": "...",
  "n3_activated": bool,
  "n6_activated": bool,
  "executed_nodes": [...],
  "skipped_nodes": [...],
  "spawn_count": <int>,
  "spawn_budget_used_ratio": "<n>/<limit>",
  "wall_clock_seconds": <int>,
  "wall_clock_target_seconds": <int>,
  "wall_clock_advisory": "WITHIN_TARGET" | "EXCEEDED_SOFT" | "HALTED_HARD",
  "back_edges_fired": [...],
  "forward_edges_fired": [...],
  "node_timings": {...},
  "signal_state_snapshot": {...},
  "verification_pass_count": 1 | 2,
  "synthesis_pass_count": 1 | 2,
  "supplemental_brainstorm_fired": bool
}
```

### 4.13 session.json (run-state for replay)

```json
{
  "session_id": "...",
  "skill_version": "1.0.1",
  "created_at": "ISO-8601",
  "last_updated_at": "ISO-8601",
  "scale": "STANDARD" | "DEEP",
  "flags": {
    "genius_threshold": 8,
    "drift_threshold": 3,
    "quiet": false,
    "verbose": false
  },
  "input_paths": {"node_a": "...", "node_b": "..."},
  "executed_nodes": [...],
  "signal_state": {                  // incremental persistence for --resume
    "(N1, intake_digest)": {...},
    "(N2a, analysis_a_digest)": {...}
  },
  "back_edges_enqueued": [...],      // for resume — tracks back-edge state across interrupts
  "failed_spawns": [
    {"node_id": "...", "error_msg": "...", "timestamp": "...", "retry_eligible": true, "retry_count": 0}
  ],
  "halt_reason": null | "user_interrupt" | "wall_clock_hard" | "spawn_cascade" | "inline_node_failure",
  "verbose_trace": [...]
}
```

**Persistence cadence:** session.json is rewritten after each node completes (atomic write: write-to-temp + rename). This ensures `--resume` always has a coherent snapshot to load from. SIGNAL_STATE is the load-bearing field for resume; everything else can be reconstructed from stage files if needed.

**Skill version compatibility on `--resume`:**
- Major version match required (1.x ↔ 1.x OK; 1.x ↔ 2.x → HALT with "skill version mismatch: cannot resume 1.x session under 2.x skill")
- Minor version mismatch: allowed with advisory ("Resuming 1.0 session under 1.1 skill — behavior may differ slightly")
- Patch version mismatch: silent (1.0.0 ↔ 1.0.1 OK)

### 4.14 SIGNAL_STATE Schema (in-memory, persisted to graph-trace.json)

```python
SIGNAL_STATE: Dict[Tuple[node_id, signal_field], Payload]
Payload = {
    "value": <field-specific>,
    "signal_flags": [<flag_id>, ...],
    "timestamp": "ISO-8601",
    "source_node": <node_id>
}
```

**Declared signal_field enum** (every active edge's signal_field must match):
`intake_digest, analysis_a_digest, analysis_b_digest, tailored_b_digest, xref_map, ideas_digest, accepted_ideas_digest, breakthrough_digest, falsification_result, adversarial_digest, falsification_digest, solutions_digest, enhanced_draft, first_pass_verified, expansion_digest, gate:S2_thin_B, gate:S7_no_alternatives ∧ ¬N6_ran, gate:S11_artifact_gap, —`

### 4.15 Per-Node Stage File Schema

```yaml
---
node_id: "N<id>"
node_name: "<canonical>"
exec_type: "inline" | "spawn"
hat: "<persona>"
started_at: "ISO-8601"
completed_at: "ISO-8601"
duration_ms: <int>
context_lines_used: <int>           # lines in the node's prompt + generated output combined
context_budget_lines: <int>         # declared budget per §2.1
signal_flags_raised: [...]          # subset of node's declared raises_signals; default []
status: "complete" | "incomplete"
---
```

**`context_lines_used` measurement:** sum of lines in (a) the prompt assembled for the node (prompt template + KB content + upstream digests) and (b) the generated output written to the stage file body. This is an approximate budget metric for tuning; not a hard cap (unless `>1.2 × context_budget_lines`, in which case advisory logged).

Body: required output sections per node (declared in graph.json). Every required section MUST be present and non-empty for `status: complete`. Failed/partial executions write the stage file with `status: incomplete` and whatever sections were produced before failure.

### 4.16 dropped-by-budget.md (N8, conditional)

Written by N8 when the wall-clock budget defer triggers (per §5.5). Lists accepted ideas that were not engineered into solutions because budget exhausted before processing them.

```yaml
---
session_id: "..."
trigger: "approaching_75pct_budget" | "n8_hard_halt"
processed_count: <int>
deferred_count: <int>
budget_used_at_defer: "<seconds_used>/<budget_seconds>"
---
```

Per-deferred entry:
```
- source_idea_id: I0X
- target_section_in_a: <ref>
- relevance_score: <int>  # carried from accepted-ideas-catalog
- reason: "approaching_budget" | "hard_halt"
- recommended_action: "manual_engineer" | "next_session_priority"
```

This artifact is honest reporting — not a failure mode per se, but a signal that the input was richer than the budget allowed. V6 still passes (mandatory artifacts present); the verdict is at most PARTIAL with explicit note in self-audit.

---

## Section 5 — N8 Hybrid Protocol Detail

The most consequential cognitive operation in the pipeline. New HG-3.

### 5.1 Inputs

From SIGNAL_STATE: `accepted_ideas_digest` (E14), `xref_map` (E12), `breakthrough_digest` (E16, optional), `adversarial_digest` (E19, optional), `falsification_digest` (E21), `intake_digest` (for S1).

From disk: `kb/oppositional-drafting-axes.md`, `kb/verification-gates.md`, `{session_dir}/input-a.md`.

### 5.2 Iteration Count Rule

| Mode | S1_complexity | Iterations after winner-pick |
|---|---|---|
| STANDARD | low | **1** (refined_v1 = final) |
| STANDARD | medium | **1** |
| STANDARD | high | **2** (refined_v2 = final) |
| DEEP | any | **2** |

### 5.3 Oppositional Drafting Axes

Defined in `kb/oppositional-drafting-axes.md`. 7 axes; selection algorithm picks per-idea.

| Axis | Pole 1 | Pole 2 | When |
|---|---|---|---|
| **A0** (default fallback) | Minimal-change | Ambitious-restructure | No clear domain pull |
| A1 | Preserve-original-voice | Rewrite-for-clarity | Touches phrasing/style |
| A2 | Conservative-scope | Expansive-scope | Scope ambiguous |
| A3 | Strict-constraint-preservation | Constraint-relaxation | Creates constraint tension |
| A4 | Abstract-principle | Specific-recipe | Principle-vs-procedure choice |
| A5 | Risk-averse | Risk-seeking | Uncertain payoff |
| A6 | Inline-integration | Sidebar-integration | Format ambiguous |

**Selection algorithm** (most-specific first; A0 fallback):

```
function pick_axis(idea, target_section, node_a_text):
    if idea.touches_phrasing_or_style:           return A1
    elif idea.scope_is_ambiguous:                return A2
    elif idea.creates_constraint_tension:        return A3
    elif idea.is_principle_vs_procedure_choice:  return A4
    elif idea.has_uncertain_payoff:              return A5
    elif idea.target_section_format_ambiguous:   return A6
    else:                                        return A0
```

### 5.4 Step-by-Step Protocol

For each accepted idea `I_n`:

1. **Pick axis** (selection algorithm above).
2. **Draft A** along pole 1 — role-switched block adopts pole-1 stance.
3. **Draft B** along pole 2 — fresh role-switched block with **Pattern α framing**: *"Forget Draft A entirely. You are drafting fresh from the idea description and pole-2 framing only. Do not echo, contrast, or reference Draft A."*
4. **Compare** drafts using `verification-gates.md` rubric. Record `comparison_rationale` naming each draft's strengths. Pick `winner ∈ {A, B}`.
5. **Critique winner** against: original B-finding, Node A constraint, loser's strengths, counter-examples from `adversarial_digest` (if available).
6. **Refine v1** addressing critique.
7. **(Conditional iteration 2)**: if iteration count = 2:
   - 7a. `critique_2`. If "no actionable defects," mark and use refined_v1 as final.
   - 7b. `refined_v2` addressing critique_2.
8. **Final** = refined_v2 (if exists) else refined_v1.
9. **Score**: pro, con, utility ∈ {high, medium, low}, status ∈ {ACCEPT, ACCEPT-CONDITIONAL, REJECT, DROPPED}.
10. **Append** full entry to `solution-catalog.md`.

### 5.5 Wall-Clock Budget for N8

- 1 iteration: ~30–60 sec/solution
- 2 iterations: ~50–90 sec/solution
- Typical run 6–12 solutions
- N8 total: ~3–18 min (mode + complexity dependent)
- **Per-module hard halt: 15 min (special case from global 10 min)**
- If approaching 75% of budget at <100% of solutions: defer remaining accepted ideas to `dropped-by-budget.md` rather than hard-halt silently

### 5.6 Failure Modes

| Failure | Handling |
|---|---|
| No applicable axis | Use A0 fallback |
| Both drafts equivalently weak | DROPPED — `both_drafts_inadequate` |
| Critique surfaces wrong-direction | DROPPED — `wrong_direction`; record both drafts and critique |
| `refined_v2` strictly worse than `refined_v1` | Use refined_v1 as final; record decision |
| Context budget exhausted mid-protocol | Halt with PARTIAL; remaining ideas DROPPED |

---

## Section 6 — Hard Gates & Anti-Patterns

### 6.1 Hard Gates (HG-1 through HG-5)

Absolute constraints. Violation halts the skill.

#### HG-1 — Output ≠ Input (path divergence)
N1 verifies `enhanced.md` target path ≠ Node A source path. Trivially satisfied for inline Node A (recorded as `node_a_source: 'inline'`). HALT on equality.

#### HG-2 — Self-contained runtime
Once N1 starts, no node performs:
- Web search, web fetch, scrape, MCP query (including Dify KBs)
- `grep`, `glob`, filesystem traversal beyond allowed scope
- User-facing questions

**Allowed read scope**: `{session_dir} ∪ {skill_path} ∪ {node_a_source_path} ∪ {node_b_source_path}`. Source paths read once at N1 only.

**Design-time exemption**: KB content informed by Dify queries at skill-design-time, snapshotted into `kb/*.md`. No runtime queries.

#### HG-3 — Hybrid protocol per solution
Every entry in `solution-catalog.md` with `status ∈ {ACCEPT, ACCEPT-CONDITIONAL}` must have:
1. `oppositional_axis` named
2. `draft_A`, `draft_B` both present
3. `comparison_rationale`, `winner`, `critique_1`, `refined_v1`
4. When iterations=2: `critique_2` always required; `refined_v2` required UNLESS `critique_2.verdict = 'no_actionable_defects'`
5. `final` field set

#### HG-4 — Fresh copy only
All file writes go to `{session_dir}/`. No writes to:
- Node A or Node B source directories
- `{skill_path}/` (read-only at runtime)
- Paths containing `..` traversal

#### HG-5 — Bounded iteration

| Iteration site | Cap |
|---|---|
| N5 lateral passes | ≤3 (driven by S4) |
| N5.5 supplemental brainstorm | ≤1/run |
| N8 critique-and-refine | 1 or 2/solution per §5.2 |
| Back-edge E09 | ≤1/run |
| Back-edge E20 | ≤1/run |
| Back-edge E26 | ≤1/run |
| Forward-conditional E25 | ≤1/run |
| Pipeline | ≤2 passes (first + optional expansion) |

### 6.2 Process Rules

- **PRC1** (§3.5): pre-execution topology validation
- **PRC2** (§4.14): SIGNAL_STATE append-only per `(node_id, signal_field)` key

### 6.3 Anti-Patterns (AP-1 through AP-20)

**Source preservation:**
1. DO NOT remove, paraphrase, or summarize Node A directives — verbatim preservation
2. DO NOT remove, paraphrase, or summarize Node B findings
3. DO NOT cause regression — enhanced never has less information than original
4. DO NOT overwrite original Node A (restates HG-4)

**Runtime discipline:**
5. DO NOT introduce infinite loops (restates HG-5)
6. DO NOT let runtime ask user questions or do external research (restates HG-2)
7. DO NOT assume literal persistent database — markdown + SIGNAL_STATE dict
8. DO NOT modify graph.json mid-run

**Cognitive protocol:**
9. DO NOT generate solutions outside N8 hybrid protocol (restates HG-3)
10. DO NOT treat Alternative Hypotheses as multiple competing ideas — only best-fit
11. DO NOT pick section list at runtime by guessing — use Genius Detection + Section-Tailoring Map
12. DO NOT process Section-Tailoring Map sections out of order — Headline Insight FIRST
13. DO NOT skip critique-and-refine in N8 when iteration count is configured
14. DO NOT echo, contrast, or reference Draft A while drafting Draft B (Pattern α framing)

**Topology discipline:**
15. DO NOT exceed orchestrator-level spawn budget — STANDARD ≤6, DEEP ≤7
16. DO NOT fire any back-edge more than once per run
17. DO NOT trigger expansion (E25) on N11's second verification pass — single-firing cap
18. DO NOT overwrite `enhanced.md` without preserving first-pass copy when E26 fires
19. DO NOT run nodes whose activation conditions are not satisfied. The orchestrator MUST honor gate conditions (E08, E25) and signal flags (S2, S7, S11) rather than running nodes "just in case." Specifically: N3 must NOT run if E08's gate is false (Node B is generic-fallback); N12 must NOT run if S11_artifact_gap is not raised.
20. DO NOT run analysis sequentially when ready-set scheduling would parallelize — N2a ‖ N2b dispatched in single Agent message

### 6.4 Behavior on Violation

| Violation | Response |
|---|---|
| HG-1, HG-4 (path) | HALT before any work; exit non-zero |
| HG-2 (runtime escape) | HALT with advisory naming offending operation |
| HG-3 (incomplete trail) | V1 fails; affected solutions `DROPPED — hg3_violation`; verdict at least PARTIAL |
| HG-5 (loop cap) | HALT mid-iteration; PARTIAL artifacts |
| PRC1 failure | HALT before execution |
| PRC2 violation | HALT mid-pipeline; PARTIAL artifacts |
| AP-1, AP-2, AP-3 (paraphrase/regression) | V4 fails; verdict PARTIAL; flagged in self-audit |
| AP-15 (spawn budget) | HALT before exceeding dispatch |
| AP-16, AP-17 (back-edge cap) | HALT mid-pipeline |
| AP-18 | N10 self-corrects (preserves first-pass) — internal enforcement, not external violation |

---

## Section 7 — Verification Battery (V1–V8)

### 7.1 Composition

V1–V6 inherit from epiphany-analysis's R1–R6 (renamed and refined). V7–V8 are GoT-specific.

| ID | Name | Class |
|---|---|---|
| V1 | Hybrid trail completeness | soft |
| V2 | Solution target verifiability | soft |
| V3 | Cross-solution non-contradiction | soft |
| V4 | Node A constraint preservation | soft |
| V5 | Output well-formedness | **hard** |
| V6 | Artifact completeness + thin-spot detection | soft (completeness) + signal-raising (thin-spot) |
| V7 | Graph-trace edge integrity | **hard** |
| V8 | Topology-driven execution | **hard** |

**Hard checks** elevate verdict directly to FAIL on any failure. **Soft checks** yield PARTIAL on partial failure.

### 7.2 Per-Check Specifications

#### V1 — Hybrid trail completeness
Every accepted/conditional solution has all HG-3 fields per §6.1. >50% failures → verdict FAIL.

#### V2 — Solution target verifiability
`target_section_in_a` references a section that exists in `input-a.md` (regex on markdown headers + `§N.M` references). Custom HTML anchors → V2 advisory (not FAIL). Hallucinated targets → solution `DROPPED — v2_hallucinated_target`.

#### V3 — Cross-solution non-contradiction
N10 pre-write contradiction check resolves first (merge-or-drop, logged in `synthesis-decisions.md`). N11 V3 re-checks; surface contradictions remaining → PARTIAL with logged pair for human review.

#### V4 — Node A constraint preservation
Every imperative/constraint in `input-a.md` (MUST/SHALL/REQUIRED/FORBIDDEN keywords + enumerated bullets) appears verbatim or fuzzy-matched (Levenshtein <10%) in `enhanced.md`. Missing constraints listed in self-audit. DEEP can trigger re-synthesis via S11.

#### V5 — Output well-formedness
N10 self-validates pre-write (1 retry). V5 catches anything missed. FAIL → verdict FAIL with PARTIAL artifacts and user-actionable error.

#### V6 — Artifact completeness + thin-spot detection
**Completeness**: all mandatory artifacts present with current timestamps:
- `enhanced.md`, `analysis-report.md`, `ideas-catalog.md`, `accepted-ideas-catalog.md`, `solution-catalog.md`, `synthesis-decisions.md`, `self-audit.md`, `signal-trace.md`, `graph-trace.json`
- DEEP-conditional: `expansion-pass.md` if N12 ran; `enhanced-first-pass.md` if E26 fired
- All executed nodes' `stages/N*.md` files

**Thin-spot detection (DEEP, pass=1 only)**: if `solution_count / xref_count < 0.6`, raises S11_artifact_gap → E25 → N12.

#### V7 — Graph-trace edge integrity
`forward_edges_fired ∪ back_edges_fired ⊆ graph.json.declared_edges`. Phantom edges → FAIL.

#### V8 — Topology-driven execution
Every executed node's activation must trace to one of these documented activation paths:
1. **Required edge satisfied** — all required edges from active source nodes have SIGNAL_STATE entries
2. **Forward-conditional gate true** — gate condition (E08 or E25) evaluated true at activation time
3. **Back-edge enqueued by signal trigger** — E09 (S2_thin_B), E20 (S7_no_alternatives ∧ ¬N6_ran), or E26 (post-N12-completion) — each via the documented enqueue rule
4. **Sync-rule pending-required addition** — N6 added to N8's pending-required set on E20 firing (early/late back-edge sync rules in §3.3)

Anything else (e.g., N6 in `executed_nodes` without S2_thin_B/S7_no_alternatives raised; N12 in `executed_nodes` without S11_artifact_gap raised) → FAIL with `improvisation_detected: <node_id, missing_activation_path>`.

Note: V8 is a **scheduler-correctness check**, not a design-time check. It catches orchestrator implementation bugs, not graph design issues (PRC1 covers those).

### 7.3 Verdict Aggregation

```
fails = count(checks with status=FAIL)
partials = count(checks with status=PARTIAL)

if any of {V5, V7, V8} fail: verdict = FAIL
elif fails == 0 and partials == 0: verdict = PASS
elif fails == 0: verdict = PARTIAL
else: verdict = FAIL
```

### 7.4 Verification Pass Counting

- **First pass** MAY raise S11_artifact_gap (DEEP only) → triggers expansion loop.
- **Second pass** MUST NOT raise S11 (single-firing cap on E25 prevents re-trigger).
- Second pass exits via E24 regardless of remaining thin spots; logged as PARTIAL with note `expansion_did_not_resolve_thin_spots` if applicable.

---

## Section 8 — Mode Behavior, Edge Cases, Errors

### 8.1 Mode Behavior

#### STANDARD (default, no flag)

Active topology: 13 nodes (all except N12). Active edges: E01–E24 (E25/E26 inactive). Wall-clock soft target ≤22 min; hard halt 35 min. Spawn count 5; budget ≤6.

Per-input-type:
- **genius-current**: full pipeline; N3 active; N6 typically not fired
- **genius-drift**: same; N3 processes matched canonical sections; unmatched flow as flat
- **generic-fallback**: N3 skipped (E08 false); N2b raises S2_thin_B → N6 runs early
- **missing**: N1 generates inline structural analysis; advisory emitted; treated as `inline-substitute`

#### DEEP (`--deep`)

Active topology: 14 nodes. Active edges: E01–E26. Wall-clock soft target ≤38 min; hard halt 55 min. N8 always 2 iterations regardless of S1.

**Expansion path**: N10 first synth → N11 → if S11 raised, E25 → N12 → E26 → N10 renames first-pass → N10 re-synth → N11 second pass (no S11 possible) → E24 → output.

#### Mode and flag rejection

| Invalid combination | HALT message |
|---|---|
| `--minimal` | "--minimal mode is no longer supported. Use /epiphany-analysis for fast-path runs, or run without flag for STANDARD mode." |
| Two of `{--standard, --deep}` | "Conflicting scale flags." |
| Unknown flag | "Unknown flag: `<flag>`." |
| `--resume` without `<session_dir>` arg | "Resume requires session directory argument." |
| `--resume` with scale mismatch vs saved session | "Resume scale conflicts with saved session.scale = `<X>`." |
| `--resume` with skill major-version mismatch | "Skill version mismatch: cannot resume `<saved_version>` session under `<current_version>` skill." |
| Effective `genius_threshold ≤ drift_threshold` | "Genius threshold (`<g>`) must exceed drift threshold (`<d>`)." |
| `--genius-threshold ≤ 0` | "Genius threshold must be positive." |
| `--genius-threshold > 10` | Advisory (not HALT): "Genius threshold (`<g>`) exceeds canonical section count (10); all Node B inputs will classify as generic-fallback." Continue. |
| `--quiet` AND `--verbose` | "Conflicting verbosity flags." |
| `--retry-failed` without `<session_dir>` arg | "Retry-failed requires session directory argument." |
| `--retry-failed` on session with `failed_spawns: []` and `halt_reason: null` | Advisory: "Session is complete; no failed spawns to retry." Exit 0. |

### 8.2 Edge Cases

| Case | Behavior |
|---|---|
| (a) Node B missing | N1 generates inline structural analysis; advisory; node_b_type='inline-substitute' |
| (b) Node B empty file | Same as (a) |
| (c) Node B not an analysis | N1 classifies generic-fallback; E08 false → N3 skipped; S2_thin_B → E09 → N6 |
| (d) Node A and Node B same file | HG-1 HALT in N1 |
| (e) All ideas rejected by N5.5 | S5 fires; supplemental fires; if still none, advisory. N8 writes empty solution-catalog. N10 produces unchanged enhanced.md + "No solutions accepted" note |
| (f) Spawn budget exhausted | Pre-check before each dispatch HALTs with `spawn_budget_exhausted` |
| (g) S2 and S7 both raised | E09 fires first; E20 sees N6 ∈ executed_nodes → does not fire |
| (h) Two solutions contradict on same section | N10 pre-write check resolves (merge or drop-lower-utility); logged in synthesis-decisions.md |
| (i) DEEP expansion no improvement | V6 second-pass detects; verdict PARTIAL with note `expansion_did_not_resolve_thin_spots` |
| (j) Node A very large (>5000 lines) | N1 advisory; S1=high; N2a/N8 may produce structural-only/dropped solutions; verdict PARTIAL with complexity note |
| (k) Node A binary or non-text | N1 HALT with "Node A must be text content" |
| (l) Inline Node A | HG-1 trivially OK; `node_a_source: 'inline'` |
| (m) Node B is itself a prior enhanced output | Treated normally; no recursion concern |
| (n) Spawn returns malformed output | Post-spawn parser fails; treated as spawn failure (§8.3); `error_msg='malformed_output: <details>'` |
| (o) Re-invoke with same Node A on same day | session-init.sh detects existing session_dir matching `<date>-<hash>` pattern; auto-suffixes `-2`, `-3`, etc. for the new session. Original session preserved. To intentionally reuse, pass `--resume <session_dir>` instead. |
| (p) Both Node A and Node B inline | Both stored verbatim; HG-1 trivially satisfied (no source paths). Frontmatter records `node_a_source: 'inline'` and `node_b_source: 'inline'`. |
| (q) Node A with no detectable structure (no markdown headers, all prose) | N1 sets `structural_depth=1`. S1 formula still computes; complexity bucket likely `low` or `medium`. N3 likely skipped (Node B may still have structure). Pipeline proceeds normally. |

### 8.3 Spawn Failure Handling

1. Log warning with node ID, error type, message, timestamp.
2. Do NOT write SIGNAL_STATE for failed node.
3. Do NOT add to `executed_nodes`.
4. Add to `session.json.failed_spawns`: `{node_id, error_msg, timestamp, retry_eligible}`. Default `retry_eligible=true`; flips to `false` after 2 retries fail.
5. Cascade-block dependents (any node with required edge from failed node).
6. Continue ready-set evaluation for unaffected nodes.
7. If cascade empties ready-set before N10 completes: HALT with `halt_reason: spawn_cascade`; PARTIAL artifacts.

### 8.3.1 Inline Node Failures

If an inline node fails (context overflow, malformed output, role-switched-block parse failure):
- HALT with `halt_reason: inline_node_failure`.
- Write current artifacts with PARTIAL status.
- Failed node's stage file written with `status: incomplete`.
- User can investigate, fix root cause, `--resume` from failed node.

### 8.4 Hard Halt Strategy

| Trigger | Action |
|---|---|
| Per-module hard halt (10 min, 15 min for N8) | Halt that node; cascade based on whether output is required |
| Mode wall-clock hard halt (35 STANDARD / 55 DEEP) | Halt entire pipeline; PARTIAL artifacts; `halt_reason: wall_clock_hard` |
| User interrupt (SIGINT) | Halt gracefully on next ready-set boundary; PARTIAL; `halt_reason: user_interrupt` |
| Spawn cascade exhaustion | Per §8.3 |
| PRC1 failure | HALT before execution; no artifacts |
| HG-1 violation | HALT in N1; minimal artifacts |

### 8.5 `--retry-failed <session_dir>`

1. Load `session.json`; validate skill_version match.
2. For each `failed_spawns` entry with `retry_eligible: true`: re-dispatch; on success update state; on failure increment counter, flip eligibility after 2 retries.
3. Resume ready-set scheduling; cascade-blocked nodes may become eligible.
4. Continue to N11 → output.
5. Empty `failed_spawns`: advisory "Session is complete; no failed spawns to retry."

### 8.6 `--resume <session_dir>`

1. Load `session.json`; validate skill_version, scale match.
2. Resume ready-set scheduling from `executed_nodes` state. Already-executed nodes NOT re-run.
3. Continue normal pipeline.

**Mixed state handling**: If `failed_spawns` non-empty, advisory: *"Session has N failed spawn(s). Use --retry-failed to retry them, or --resume to continue without retry (failed spawns will cascade-block dependents)."* User chooses.

### 8.7 Wall-Clock Advisory Tiers

`graph-trace.json.wall_clock_advisory`:
- `WITHIN_TARGET` — ≤ soft target
- `EXCEEDED_SOFT` — > soft, < hard halt
- `HALTED_HARD` — reached hard halt; PARTIAL artifacts

**Measurement methodology:** `wall_clock_seconds` is measured from the moment N1 begins execution (after PRC1 validation completes) to the moment N11 writes its final terminal output (E24 fires) — or the moment the orchestrator halts, whichever comes first. Pre-execution overhead (flag parsing, PRC1, session-init.sh) is excluded; this is the cognitive-work timer, not the total CLI invocation time. Per-node timings in `graph-trace.json.node_timings` use the same start/end semantics per-node (start = orchestrator dispatched the work; end = orchestrator received and persisted the output).

### 8.8 Determinism Note

The skill is **structurally deterministic** with respect to (Node A, Node B, scale, flags): same inputs produce the same `executed_nodes` set, same `back_edges_fired` set, same `signal_state_snapshot` keys, same V1–V8 verdict structure. Solution and synthesis text content varies between runs (LLM stochasticity); the topology and signal-routing decisions are reproducible.

---

## Section 9 — Testing & Implementation Phases

### 9.1 Testing Strategy

#### 9.1.1 Unit Tests (`tests/unit/`)
- `validate-graph.py` against well-formed and intentionally-broken graph.json
- `session-init.sh` directory creation and ID generation
- Frontmatter parser round-trip
- SIGNAL_STATE writer (PRC2 enforcement)

#### 9.1.2 Smoke Tests (mode + Node B types)
- `smoke_standard_genius_b`, `smoke_standard_drift_b`, `smoke_standard_generic_b`, `smoke_standard_no_b`
- `smoke_deep_no_expansion`, `smoke_deep_with_expansion`, `smoke_deep_supplemental_brainstorm`

#### 9.1.3 Signal Routing Tests
- `signal_s2_thin_b`, `signal_s7_no_alternatives_n6_not_run`, `signal_s7_no_alternatives_n6_already_run`, `signal_s11_artifact_gap_standard`, `signal_s11_artifact_gap_deep`, `signal_s5_idea_pool_thin`, `signal_s4_xref_density_sparse`
- **Tuning signal tests**: `signal_s1_high_triggers_extra_iteration`, `signal_s4_sparse_increases_lateral_passes`, `signal_s4_dense_decreases_lateral_passes`

#### 9.1.4 Hard Gate & PRC Tests
- `hg1_path_equality`, `hg1_inline_node_a`, `hg2_runtime_escape`, `hg3_incomplete_solution`, `hg4_outside_session_write`, `hg5_back_edge_double_fire`
- `prc1_invalid_graph`, `prc2_signal_state_rewrite`

#### 9.1.5 Verification Battery Tests (V1–V8)
- One synthetic injection test per check; verifies detection works as specified

#### 9.1.6 Regression Tests
- `regression_vs_epiphany_analysis`: same (A,B) inputs through both skills; compare solution_count parity, constraint preservation, structural overlap
- `regression_vs_graph_genius`: reuse PRC1 fixtures with adjusted node IDs

#### 9.1.7 Replay & Resume Tests
- `replay_complete_session`, `resume_after_interrupt`, `retry_failed_after_spawn_fail`, `retry_failed_with_clean_session`, `resume_with_unretried_failures`

### 9.2 Implementation Phases

Six phases; each has a deliverable and acceptance criterion. Effort: ~10.5 days raw + 30% buffer = ~14 working days.

#### Phase 1 — Design-Time Research (consolidation) [0.5 day]
**Deliverable:** This design doc. **Acceptance:** referenced precedents explicitly; explained every divergence.

#### Phase 2 — Graph Topology + Schema [1 day]
**Deliverable:** `graph.json`, `graph.schema.json`, `scripts/validate-graph.py`. **Acceptance:** validate-graph.py exits 0 on well-formed; non-zero with explanatory error on broken inputs.

#### Phase 3 — Module Protocols [4 days]
**Deliverable:** 14 files in `modules/N*.md`. Per-node frontmatter (schema in **Appendix D**) + PROTOCOL section. N8 includes full hybrid protocol; N1 includes HG-1 + S1 + genius detection (canonical sections from **Appendix C**); N10 includes pre-write contradiction + well-formedness checks + first-pass preservation; N11 includes V1–V8 + first/second-pass detection. **Acceptance:** every node's required output sections appear in PROTOCOL output spec; module frontmatter validates against Appendix D schema; manual review.

#### Phase 4 — KB Assembly [1 day]
**Deliverable:** 12 KB files. 8 ported from graph-genius (domain-catalog, debono-techniques, ohlsson-defixation, falsification-checklists, verification-gates, blend-template, elegance-rubric, vocabulary-rubric); 3 from epiphany-analysis (section-tailoring-map, input-preloading-templates, analysis-methodology); 1 new (oppositional-drafting-axes). **Acceptance:** every KB reference in graph.json resolves.

#### Phase 5 — Orchestrator & Scripts [2 days]
**Deliverable:** `SKILL.md` + `scripts/session-init.sh` + `scripts/test-runner.sh`. SKILL.md implements: STEP 0 (flag parse + PRC1) → STEP 1 (session init) → STEP 2 (ready-set scheduler) → STEP 3 (artifact assembly) → STEP 4 (summary). Scheduler implements inline-to-fixpoint + parallel spawn dispatch + back-edge enqueue + cap enforcement + SIGNAL_STATE PRC2. **Acceptance:** SKILL.md complete; `test-runner.sh --unit` 100% pass.

#### Phase 6 — Test Battery & Final Validation [2 days]
**Deliverable:** complete `tests/`. Authors all fixtures, runs all suites, records `tests/results/<date>-results.md`. **Acceptance:** §9.1.4 (HG/PRC) 100% PASS, §9.1.5 (V1–V8) 100% PASS, ≥90% smoke + signal-routing PASS.

### 9.3 Out of Scope (deferred to v1.1)

- N8 as spawn (Decision 4b alternative)
- Pattern β true isolation for N8 drafts
- Configurable filter threshold (`--filter-threshold` flag)
- Configurable V4 fuzzy threshold (`--v4-fuzzy-threshold` flag)
- `--retry-failed` implicit retry inside `--resume`
- Custom HTML anchor support in V2
- Sub-spawn for N6 internal fan-out
- Direct anti-pattern tests (covered transitively by HG/PRC/V tests)

---

## Appendix A — Design Departures from prompt-graph Spec

For traceability, this design departs from `~/docs/epiphany/prompts/24-04-epiphany-graph-analysis-skill-spec.md` in the following ways. Each departure was discussed and approved during the brainstorming session.

| # | Departure | Rationale |
|---|---|---|
| 1 | N5.5 IdeaFilter inserted between N5 and N8 | User intent: separate "ideas cataloged" from "solutions cataloged" — three artifacts (ideas, accepted-ideas, solutions) |
| 2 | N2 split into N2a + N2b | Cleaner topology; truly independent A and B analyses; richer per-node stage files; +1 spawn (within increased budget) |
| 3 | MINIMAL mode dropped | MINIMAL didn't exercise GoT topology; user has /epiphany-analysis for fast-path |
| 4 | N8 hybrid protocol (oppositional drafts → iterate) | User question reframed HG-3; literature supports refinement for engineering tasks; hybrid combines diversity (branching) + quality (iteration) |
| 5 | Spawn budget increased: STANDARD ≤6, DEEP ≤7 | Accommodates N2 split; aligns with quality-over-spawn-savings priority |
| 6 | Wall-clock budget increased: STANDARD 22/35, DEEP 38/55 | Accommodates N8 hybrid + extra signals |
| 7 | 6 signals (was 3): added S1, S4, S5 | Tuning signals exploit GoT for adaptive depth; S5 supports inline supplemental brainstorm |
| 8 | V1–V8 verification battery (was R1–R6) | Adds GoT-specific topology integrity (V7) and topology-driven execution (V8) checks |
| 9 | `--resume` and `--retry-failed` flags added | Inherited from graph-genius pattern; replay-friendly operation |
| 10 | `synthesis-decisions.md` artifact added | Records N10 merge/drop decisions; replaces `merged_into` field |
| 11 | Per-node stage files elevated to first-class artifacts | User intent: "full analysis at each step cataloged" |
| 12 | `signal-trace.md` artifact added | Human-readable signal narrative complements structured graph-trace.json |
| 13 | N8 context budget bumped 1500 → 2000 lines | Hybrid protocol's cognitive load with continuous-context across solutions |
| 14 | E25 single-firing cap explicit | Prevents infinite expansion-verify loop |
| 15 | KB expanded by 3 files (elegance-rubric, vocabulary-rubric, blend-template) | Added precision-forcing and ideation-quality rubrics |

### v1.0.1 audit additions (post-brainstorming review)

| # | Addition | Why |
|---|---|---|
| 16 | Canonical section list with synonyms (Appendix C) | Genius detection unimplementable without it |
| 17 | S1 complexity formula → bucket cutoffs (`log10(line_count) × structural_depth × (1 + constraint_density)`) | Formula was named but unbucketed |
| 18 | N5.5 score ranges (1–5) and ACCEPT/CONDITIONAL/REJECT thresholds | Filter outputs were undefined |
| 19 | `dropped-by-budget.md` artifact + schema (§4.16) | Mentioned but not declared in v1.0.0 |
| 20 | SIGNAL_STATE incremental persistence to session.json | Required for `--resume` correctness |
| 21 | Re-run into existing session_dir auto-suffix behavior | Behavior was undefined |
| 22 | Spawn count math correction for DEEP+E26 (6 dispatches, not 5) | N10 re-synth on expansion was uncounted |
| 23 | Module file frontmatter schema (Appendix D) | Implementation gap |
| 24 | Error message catalog (Appendix E) | Inconsistent message format risk |
| 25 | Quick Reference section + cost estimate per mode | UX improvement for skill readers |
| 26 | N7 stage file schema (§4.7.1) | adversarial_digest payload was undefined |
| 27 | N12 expansion proposal structured format (typed `proposed_depth_pass`) | Vague text instructions wouldn't be N10-actionable |
| 28 | V8 documented activation paths + sync-rule recognition | "No improvisation" was undefined |
| 29 | Flag combinations matrix (--quiet+--verbose, threshold edge cases) | Uncovered cases |
| 30 | Wall-clock measurement methodology | Reproducibility |

---

## Appendix B — Verification Battery Origin Map

| Check | Origin | Notes |
|---|---|---|
| V1 | epiphany-analysis R1 | Revised for HG-3 hybrid protocol |
| V2 | epiphany-analysis R2 | Markdown-anchor + numeric-ref support; custom anchors → advisory |
| V3 | epiphany-analysis R3 | N10 pre-write resolves; V3 audits |
| V4 | epiphany-analysis R4 | Levenshtein <10% fuzzy match |
| V5 | epiphany-analysis R5 | N10 self-validates pre-write |
| V6 | epiphany-analysis R6 | Expanded artifact list; thin-spot detection signals S11 |
| V7 | NEW (GoT-specific) | Phantom edge detection |
| V8 | NEW (GoT-specific) | No orchestrator improvisation |

---

## Appendix C — Canonical Section List & Synonym Matching

The 10 canonical sections of an epiphany-genius output. N1's Genius Detection counts case-insensitive header matches against this set (and accepted synonyms). Matching is performed on `## Header` lines first, falling back to bold-line variants (`**Header**` on own line). Trailing punctuation, leading numbering (`1.`, `(a)`), and trailing parentheticals are stripped before comparison.

| # | Canonical Header | Accepted Synonyms |
|---|---|---|
| 1 | Headline Insight | "Primary Insight", "Lead Insight", "Top-Line Insight", "Bottom Line", "Core Insight" |
| 2 | Theory Collisions | "Theoretical Conflicts", "Conflicting Claims", "Theory Conflicts", "Claim Conflicts" |
| 3 | Discovery vs. Proof | "Discovery vs Proof", "Discovery–Proof Gap", "Claim vs Evidence", "Discovery and Proof" |
| 4 | Independence-Verified Bridges | "Independence Bridges", "Verified Bridges", "Cross-Domain Bridges", "Bridges (Verified)" |
| 5 | Alternative Hypotheses | "Alt Hypotheses", "Alternative Theories", "Counter-Hypotheses", "Competing Hypotheses" |
| 6 | Density-Checked Falsification | "Falsification (Density-Checked)", "Density Falsification", "Counter-Examples", "Falsification Checks" |
| 7 | Scope Limits | "Scope Boundaries", "Applies-to / Does-not-extend-to", "Boundaries", "Domain Limits" |
| 8 | Coherence Signals | "Convergent Signals", "Coherence Indicators", "Signal Convergence", "STRONG/MODERATE/WEAK Signals" |
| 9 | Generalization Checks | "Generalization", "Holds-at / Breaks-at", "Generality Checks", "Universality Tests" |
| 10 | Open Questions & Next Probes | "Open Questions", "Next Probes", "Questions & Probes", "Future Probes", "Probes & Questions" |

**Counting rules:**
- Each canonical section counts at most once per Node B (duplicates = 1).
- Synonym match counts equally with canonical match.
- A section header followed by an empty body still counts.
- Within the same Node B, the FIRST recognized form (canonical OR synonym) wins; subsequent variants don't add to the count.

**Score → classification (using effective thresholds):**
- `score ≥ genius_threshold` (default 8) → `genius-current`
- `drift_threshold ≤ score < genius_threshold` (default 3 ≤ score < 8) → `genius-drift`
- `score < drift_threshold` (default <3) → `generic-fallback`

---

## Appendix D — Module File Frontmatter Schema

Every `modules/N*.md` file begins with this frontmatter block. Validates against `graph.json` per the source-of-truth rule.

```yaml
---
node_id: "N<id>"                    # e.g., "N5.5"
node_name: "<canonical>"            # e.g., "IdeaFilter"
module_version: "1.0.0"             # semantic version of this module file
type: <NODE_TYPE>                   # DECOMPOSE | ANALYZE-A | ANALYZE-B | TAILOR | XREF | LATERAL | FILTER | DEFIX | ADVERSARIAL | ENGINEER | ROUTER | SYNTHESIS | VERIFY | EXPANSION
exec_type: "inline" | "spawn"
hat: "<persona>"                    # e.g., "Tesla (measurement)"; "—" for N9 router
context_budget_lines: <int>         # must match graph.json
scale_gates: ["STANDARD", "DEEP"] | ["DEEP"]
activation:                         # how this node becomes eligible
  - "always"                        # OR
  - "conditional:<edge_id>"         # for conditional inline (N3, N6, N12)
raises_signals:                     # subset of declared signals; [] if none
  - <signal_id>
required_output_sections:           # MUST appear in body; non-empty
  - "<section_name>"
input_dependencies:                 # SIGNAL_STATE keys required to activate
  - "(N<id>, <signal_field>)"
optional_inputs:                    # SIGNAL_STATE keys consumed if present
  - "(N<id>, <signal_field>)"
kb_files:                           # files in {skill_path}/kb/ this node reads
  - "<filename>"
output_signal_fields:               # SIGNAL_STATE keys this node writes
  - "<signal_field>"
output_file: "stages/N<id>-<descriptive-name>.md"
hard_gates_referenced:              # which HGs this node enforces
  - "HG-1"
---

# <Node Name> — PROTOCOL

[free-form prose: role declaration, step-by-step instructions, output format examples]
```

**Validation:** the orchestrator at STEP 0 (PRC1) cross-references each module file's frontmatter against `graph.json`. On disagreement: graph.json wins, but the disagreement is logged as a warning. Critical disagreements (different `exec_type`, missing required sections) HALT.

---

## Appendix E — Error Message Catalog

All HALT and ADVISORY messages emitted by the skill, for consistency and reviewer reference.

### HALT messages

| Trigger | Message format |
|---|---|
| HG-1 violation | `FAILED: HG-1 — enhanced.md target path equals Node A source path. Output would overwrite input.` |
| HG-2 violation | `FAILED: HG-2 — runtime escape attempt: <operation> at <node_id>.` |
| HG-3 violation (N11 V1) | (no HALT — recorded as V1 fail; affected solutions DROPPED — hg3_violation; verdict ≥ PARTIAL) |
| HG-4 violation | `FAILED: HG-4 — write attempt outside session_dir: <path>.` |
| HG-5 cap exceeded | `FAILED: HG-5 — iteration cap exceeded at <site>: <count>/<cap>.` |
| PRC1 failure | `FAILED: PRC1 — <check_name>: <details>.` |
| PRC2 violation | `FAILED: PRC2 — SIGNAL_STATE re-write of (<node_id>, <signal_field>).` |
| Spawn budget | `FAILED: spawn_budget_exhausted — <count>/<limit> dispatches. Halting before <next_node>.` |
| Spawn cascade | `FAILED: spawn_cascade — ready_set empty before N10 completion. <failed_nodes>.` |
| Inline node failure | `FAILED: inline_node_failure — <node_id>: <reason>.` |
| Wall-clock hard halt | `FAILED: wall_clock_hard — <elapsed>s > <hard>s for mode=<mode>. PARTIAL artifacts written.` |
| Conflicting scale flags | `FAILED: Conflicting scale flags.` |
| Unknown flag | `FAILED: Unknown flag: <flag>.` |
| `--minimal` rejection | `FAILED: --minimal mode is no longer supported. Use /epiphany-analysis for fast-path runs, or run without flag for STANDARD mode.` |
| Threshold inversion | `FAILED: Genius threshold (<g>) must exceed drift threshold (<d>).` |
| Threshold non-positive | `FAILED: Genius threshold must be positive.` |
| Conflicting verbosity | `FAILED: Conflicting verbosity flags (--quiet AND --verbose).` |
| Resume missing arg | `FAILED: Resume requires session directory argument.` |
| Resume scale mismatch | `FAILED: Resume scale conflicts with saved session.scale = <X>.` |
| Resume version mismatch | `FAILED: Skill version mismatch: cannot resume <saved_version> session under <current_version> skill.` |
| Retry-failed missing arg | `FAILED: Retry-failed requires session directory argument.` |
| Binary Node A | `FAILED: Node A must be text content (markdown, plain text, or structured prose).` |

### ADVISORY messages (do not halt)

| Trigger | Message format |
|---|---|
| No Node B provided | `ADVISORY: No analysis document provided. Generating inline structural analysis as Node B substitute. For richer results, provide a dedicated analysis document.` |
| Genius threshold > 10 | `ADVISORY: Genius threshold (<g>) exceeds canonical section count (10); all Node B inputs will classify as generic-fallback.` |
| Resume minor-version mismatch | `ADVISORY: Resuming <saved_version> session under <current_version> skill — behavior may differ slightly.` |
| Retry-failed clean session | `ADVISORY: Session is complete; no failed spawns to retry.` |
| Resume mixed state | `ADVISORY: Session has <N> failed spawn(s). Use --retry-failed to retry them, or --resume to continue without retry (failed spawns will cascade-block dependents).` |
| Wall-clock soft exceeded | `ADVISORY: Wall-clock exceeded soft target (<elapsed>s > <soft>s). Pipeline continuing toward hard halt at <hard>s.` |
| N8 budget defer | `ADVISORY: N8 approaching budget; deferring <N> ideas to dropped-by-budget.md.` |
| V2 false-negative | `ADVISORY: V2 found custom anchors in target_section_in_a — possible false negatives.` |
| Expansion no improvement | `ADVISORY: Expansion did not resolve thin spots; verdict PARTIAL.` |
| N1 large-input | `ADVISORY: Node A is very large (<lines> lines). N2a/N8 may produce structural-only output or drop low-utility solutions.` |
| Subagent capability probe failure | `WARNING: Agent tool dispatch failed in capability probe. Pipeline will likely fail; investigate harness configuration.` |

---

*End of design (v1.0.1, audit-pass complete).*
